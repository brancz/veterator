db:
  image: postgres:9.4
  volumes:
    - /var/lib/postgresql/data/
  ports:
    - "5432"
web:
  build: .
  command: bundle exec rails s -p 5000 -b '0.0.0.0'
  volumes:
    - .:/usr/src/app
  ports:
    - "5000:5000"
  links:
    - db

