= form_for(@unit) do |f|
  - if @unit.errors.any?
    #error_explanation
      %h2
        = pluralize(@unit.errors.count, "error")
        prohibited this unit from being saved:
      %ul
        - @unit.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :name
    %br/
    = f.text_field :name
  .field
    = f.label :symbol
    %br/
    = f.text_field :symbol
  .field
    = f.label :type_id
    %br/
    = select("unit", "type_id", Type.all.collect {|t| [ t.name, t.id ] }, {include_blank: 'None'})
  .actions
    = f.submit
